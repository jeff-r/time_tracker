
<% # console %>

<div class='event-summary'>
  <div class='event-summary-row'>
    <div class="event-summary-head">
      <%= summary.date %>: <%= duration_in_hours(summary.total_minutes) %> hours
    </div>
    <div class='event-summary-details'>
      <% @recent_events.each do |event| %>
        <% if event.start_time.strftime('%Y-%m-%d') == summary.date %>
          <div class="<%= event_row_class(event) %>" >
            <% if event.is_running? %>
              <%= formatted_duration(event) %>
            <% else %>
              <%= link_to formatted_duration(event), edit_event_path(event) %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
